import { View, Text, SafeAreaView } from "react-native";

const VerificationEvent = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>VerificationEvent</Text>
      </View>
    </SafeAreaView>
  );
};

export default VerificationEvent;